# 검증 산출물 (VALIDATION.md)

> 본 템플릿을 `VALIDATION.md`로 복사한 뒤 채워주세요. **MIGRATION_PLAN.md 안에 §검증 계획 섹션으로 통합해도 OK** — PRD §1.3 참조.
>
> 핵심: "이 검증을 실제로 실행한다면 **입력 / 명령 / 기대 출력 / 실패 시 판단 기준**이 명확한가" — 발표 Q&A 3번 축에서 검증됩니다.

---

## 0. 검증 산출물 개요

| 도메인 | 검증 시나리오 수 | 형태 (택 1 이상) |
|--------|----------------:|------------------|
| <깊이 다룬 도메인 A> | <N개> | <IaC skeleton / 리허설 / 무결성 쿼리 / runbook / 비용 표 / 관측 정의 …> |
| <깊이 다룬 도메인 B> | <N개> | <…> |

**각 깊이 도메인당 최소 1개 이상**의 검증 증거가 필요합니다 (PRD §1.3).

---

## 1. <도메인 A — 예: 데이터/스토리지> 검증 시나리오

### 1.1 <시나리오 이름 — 예: RDS → NHN DB 이전 후 데이터 무결성 검증>

**목적**: <이 검증이 무엇을 확인하는가>
**언제 실행**: <단계·시점 — 예: 컷오버 직후 T+10m, 안정화 1일 후 등>
**4시간 다운타임 제약과의 연결**: <어떻게 만족시키는가>

**입력**
- <필요한 사전 조건·환경 변수·자격 증명>

**명령 / 절차**
```bash
# 예: row count 비교
psql -h <aws-rds-host> -U app -d officeagent -c "SELECT COUNT(*) FROM documents" > /tmp/aws_count.txt
psql -h <nhn-db-host> -U app -d officeagent -c "SELECT COUNT(*) FROM documents" > /tmp/nhn_count.txt
diff /tmp/aws_count.txt /tmp/nhn_count.txt
```

**기대 출력**
- diff 결과 0줄 (count 동일)
- 또는 <차이가 허용되는 조건과 그 이유>

**실패 시 판단 기준**
- diff > 0: <어떤 분기? 롤백 트리거? 재시도?>
- 명령 자체 실패 (connection refused 등): <어떤 분기?>

---

### 1.2 <시나리오 이름 — 예: S3 → Object Storage sync 차이 검증>

(동일 구조)

---

## 2. <도메인 B — 예: 운영·관측/AIOps> 검증 시나리오

### 2.1 <시나리오 이름 — 예: 알람 진단 runbook>

**목적**: ...

**입력**
- 알람명: `HighErrorRate`
- 임계 조건: 5분간 5xx > 5%

**명령 / 절차**
```bash
# 1) 최근 5분 5xx 로그 확인
nhn-cli logs query --metric=errors --range=5m

# 2) (선택) LLM 진단 호출
echo "에러 로그: $(...)" | claude -p "이 에러 패턴의 가장 가능성 높은 원인 3개와 우선 확인할 항목을 짧게 답해"
```

**기대 출력**
- 5xx 카운트 + 응답 본문 패턴
- LLM 응답: 원인 후보 3개

**실패 시 판단 기준**
- 로그 쿼리 자체 실패: <분기>
- 원인 식별 불가: <에스컬레이션 절차>

---

## 3. 가정 · 한계 · 후속 검증 계획

> "모른다"고 적을 영역은 반드시 **리스크 + 검증 계획**과 함께. 단순 미답은 PRD §불합격 기준.

| 가정/미확인 | 영향 | 리스크 | 후속 검증 계획 |
|------------|------|--------|---------------|
| <예: NHN DB의 logical replication 지원 여부 미확정> | 무중단 이전 가능 여부 | 다운타임 4h 초과 가능 | NHN 기술지원 문의·docs 추가 정독·대안(dump+restore) 검증 시나리오 준비 |

---

## 4. 재현성 · 자동화 가능성

- 본 산출물 중 **자동화 가능한 시나리오**: <시나리오 번호>
- **수동 운영이 필요한 시나리오**: <시나리오 번호> — 자동화 곤란 이유: <…>
- 정기 운영(예: 분기별 DR 훈련)에서 재활용 가능한 부분: <…>

---

## 5. 참고 자료

- 1차 자료 인용 (NHN DB docs, NHN Object Storage docs 등 URL · 캡처)
- 참고한 마이그레이션 케이스 스터디 출처
