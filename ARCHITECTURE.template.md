# 추상화 설계 (ARCHITECTURE.md)

> 본 템플릿을 `ARCHITECTURE.md`로 복사한 뒤 채워주세요. `MIGRATION_PLAN.md`가 "**무엇을·언제·왜**"라면, 본 문서는 "**어떻게 추상화할까**"를 다룹니다.

---

## 1. 추상화 목표

같은 OfficeAgent 애플리케이션을 **NHN 클라우드**와 **(장기) 오픈스택 온프레미스** 둘 다에 배포 가능하도록 설계의 어느 층을 어떻게 분리하는가.

- **공통 (provider-neutral)**: ...
- **NHN-only**: ...
- **오픈스택-only**: ...

---

## 2. 다이어그램

### 2.1 영역 분리도

<mermaid·draw.io·Excalidraw·손그림 자유. 다음 셋을 색·박스로 구분>
- 공통 영역
- NHN-only 영역
- 오픈스택-only 영역

### 2.2 같은 애플리케이션이 두 환경에서 다르게 보이는 지점

| 컴포넌트 | NHN 환경 | 오픈스택 환경 | 공통 |
|----------|----------|----------------|------|
| ... | ... | ... | ... |

---

## 3. 도구 의사결정

### 3.1 IaC 도구 선택

채택: <Terraform / OpenTofu / Pulumi / Crossplane / Ansible / 없음(설계만)>

- **왜 이것을 골랐는가**:
- **다른 후보와 단점 비교**:
  - <대안 1>: <단점>
  - <대안 2>: <단점>

### 3.2 (IaC 채택 시) 모듈 구조

```
infra/
├── modules/
│   ├── network/        # NHN VPC ↔ OpenStack Neutron 등 추상화
│   ├── compute/        # K8s 또는 VM
│   └── storage/        # Object/Block
├── nhn/                # NHN 전용 root module
└── openstack/          # OpenStack 전용 root module
```

(IaC 미채택 시) "다이어그램·문서 수준에서 어떻게 분리 설계가 입증되는가"를 적기.

### 3.3 변수·환경 분리 전략

- 공통 변수:
- NHN-only 변수:
- 오픈스택-only 변수:
- 시크릿·자격증명 관리:

---

## 4. NHN-only 종속의 솔직한 식별

오픈스택 단계에서 재작성 필요한 부분을 미리 명시. 이게 추상화 사고의 핵심 시그널입니다.

| 영역 | NHN-only 의존 | 오픈스택 대안 | 재작성 비용 |
|------|---------------|---------------|------------|
| ... | ... | ... | ... |

---

## 5. 운영 자동화·AIOps 통합 지점 (선택)

PRD 선택 산출물에 해당. 통합 PoC를 작성한다면:

- 알람 진단 → LLM 호출 시점
- 드리프트 비교 → LLM 호출 시점
- 비용 이상치 감지 → LLM 호출 시점

각 지점에서 **LLM이 의사결정 루프 안에 어떻게 들어가는가**를 시퀀스 다이어그램으로:

---

## 6. 가정·미확인 영역

> 본 문서에서 "모른다"고 적은 부분은 반드시 **리스크 + 검증 계획**과 함께.

| 가정/미확인 | 영향 범위 | 리스크 | 검증 계획 |
|------------|----------|--------|----------|
| ... | ... | ... | ... |

---

## 7. 참고 자료

- 1차 자료 인용 (NHN docs, 오픈스택 docs)
- 본인이 참고한 블로그·세션 발표 자료 (출처 명시)
